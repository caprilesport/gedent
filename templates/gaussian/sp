{#
software = "gaussian"
jobtype = "sp"
requires = ["method", "basis_set", "charge", "mult", "nprocs", "mem", "Molecule"]
description = "Single point energy"
#}
{%if solvation-%}
{%set solvent="scrf=(smd, solvent=" ~ solvent ~ ")"-%}
{%else-%}
{%set solvent=""-%}
{%endif-%}
%nproc={{ nprocs }}
%mem={{ mem }}GB
# {{ method }}/{{ basis_set }} {{ solvent }}

job title: {{ Molecule.filename }}

{{ charge }} {{ mult }}
{{ print_molecule(molecule=Molecule) }}
